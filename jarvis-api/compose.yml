services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_DB: truck-database
      POSTGRES_USER: truck-database-user
      POSTGRES_PASSWORD: truck-database-password
    ports:
      - '5432:5432'
    networks:
      - truck-network

  mailpit:
    image: axllent/mailpit
    container_name: truck-mailpit
    restart: unless-stopped
    ports:
      - '8025:8025'
      - '1025:1025'
    volumes:
      - mailpit-data:/data
    environment:
      MP_MAX_MESSAGES: 5000
      MP_DATABASE: /data/mailpit.db
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1
    networks:
      - truck-network

networks:
  truck-network:
    driver: bridge

volumes:
  mailpit-data:
    driver: local
